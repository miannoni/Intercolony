namespace Intercolony
{
    /// <summary>How an employed worker travels from the source settlement to the colony.</summary>
    public enum EmploymentArrivalTransport
    {
        /// <summary>The worker travels by ordinary caravan.</summary>
        Conventional,

        /// <summary>The worker must arrive by drop pod from a settlement able to deliver that way.</summary>
        DropPod
    }
}
