# S01 Candidate Evidence

## Candidate scope

This Candidate implements the S01 quiet-success path and adds focused long-term assertions. The
behavioral claim is not yet proven by the in-game bridge in this environment; the runtime test
attempt stopped at infrastructure setup before executing assertions.

## Exact edit surface

- `Source/Intercolony/Contracts/ContractService.cs:1275-1321` — `AdvanceAutoReady` still gates
  through `SalesOrderService.CanMarkReadyNow`, but requests the readiness transition with
  `notifyPlayer: false` at line 1315.
- `Source/Intercolony/Orders/SalesOrderService.cs:721-777` — the public two-argument
  `MarkReadyForPickup` keeps its existing notification-enabled behavior; the internal overload
  performs the same transition with an explicit notification choice.
- `Source/Intercolony/Debug/IntercolonyLongTermSelfTest.cs:413-640,1565-1590` — snapshots the
  letter stack for automated success, checks the actionable failure letter/reason and repeat
  throttle, checks direct manual-ready compatibility, and temporarily observes Important letters
  at `Everything` before restoring the player's original letter-volume setting.
- `docs/foreman/evidence/S01.md` — this Candidate Evidence record.

No other production file was edited. The pre-existing untracked `Playtesting annotations.docx`
was not touched.

## Compile gate

Working directory: `C:\dev\foreman-deployment\workspaces\GH-3\repo`.

1. `dotnet build Source/Intercolony/Intercolony.csproj` initially exited `1` during restore
   because NuGet attempted `api.nuget.org` with TLS authentication failures and the process was
   resolving a relative `.nuget\packages` folder. No compilation errors were reported.
2. With only a process-scoped package-cache fix,
   `$env:NUGET_PACKAGES = Join-Path $env:USERPROFILE '.nuget\packages'; dotnet build Source/Intercolony/Intercolony.csproj`
   exited `0`: `Build succeeded`, `0 Error(s)`, `2 Warning(s)` (`NU1900`, vulnerability-feed
   lookup unavailable over the same TLS path). The final normal build was rerun after the bridge
   attempts and exited `0` with the same result.
3. `ilspycmd -l c Assemblies/Intercolony.dll | Select-String 'DevBridge|Bridge'` reported
   `bridge types absent from final normal assembly`, confirming the final artifact is bridge-free.

## Runtime bridge attempt

Required command, from the repository:

`powershell -ExecutionPolicy Bypass -File dev.ps1 test long-term -Fresh`

Result: exit `1` from the first invocation after the documented 180-second bridge readiness
timeout. `dev.ps1` built successfully and launched RimWorld, but no connection answered at
`127.0.0.1:34117`; it archived the failure under
`C:\Users\matte\AppData\Local\Temp\Intercolony-dev-test-failures\2026-09-05-153808-long-term.txt`.
No self-test assertions ran, so pass/fail/skip counts and clean-log status were **not produced**.

Diagnostic retry:

`powershell -ExecutionPolicy Bypass -File dev.ps1 bridge`

also exited `1` after the same 180-second “bridge never answered” timeout. The game log showed
`dev bridge listening on 127.0.0.1:34117` during startup, but read-only TCP checks found no
listener afterward. The log contained no `SteamAPI.Init() failed` signal; Workshop Harmony and
Intercolony loaded. A `dev.ps1 bridge -MainMenu` diagnostic was stopped after the same listener
absence and reported `RimWorld exited before the bridge became ready` (exit `1`). RimWorld was
stopped with `powershell -ExecutionPolicy Bypass -File dev.ps1 stop` before the final normal
build. This is an environment/bridge failure, not an assertion result.

## Focused evidence status

- Automated success quietness assertion: implemented at self-test lines 488-508, but not
  executed because the bridge never reached a runnable test session.
- Unavailable-goods/actionable-reason/throttle assertion: implemented at lines 550-580, but not
  executed. The self-test temporarily sets letter volume to `Everything` only for observation and
  restores the saved value in its `finally` block; production letter-volume behavior is unchanged.
- Manual-ready compatibility assertion: implemented at lines 521-540, but not executed.
- Seller-delivery ineligibility remains covered by the existing assertion at lines 595-609, but was
  not executed in this attempt.

## Test Sensitivity / negative control

The required negative control was **not completed**. Because the bridge could not execute the
focused suite, automated success notification was not temporarily restored and no assertion
failure can honestly be reported. The Candidate source was left in the intended quiet-success
state and rebuilt normally afterward. Runtime re-verification, the negative-control failure, and
the final clean long-term run remain unobserved.

## Not observed / no Decision Record

Not observed: long-term assertion pass/fail/skip counts, a clean Player.log delta, runtime manual
letter compatibility, runtime failure-letter reason/throttling, save/load behavior, and the
required negative-control sensitivity result. No Plan intent, authoritative owner, persisted
schema, or settled product semantic was changed, so no Decision Record information is proposed.
## Recovery diagnosis (2026-09-05 continuation)

An independent precondition check later succeeded: `netstat -ano` showed
`127.0.0.1:34117 LISTENING` under RimWorld PID 14952, and a direct loopback
status request returned `ok:true` with `worldLoaded:true`, `mapLoaded:true`,
and `saveSchema:57/currentSaveSchema:57`. Steam was running (PID 22212), but
`HKCU:\\Software\\Valve\\Steam\\ActiveProcess` was absent; no credential or
operator-login remediation was attempted or required by the observed check.
The active ModsConfig listed `miannoni.intercolony`; Player.log contained the
bridge-listening line and no `SteamAPI.Init` failure. The most specific cause
of the earlier failure is therefore transient/unresolved bridge readiness, not
a proven code or credentials defect.

After that precondition was proven, the required command was started exactly:
`$env:NUGET_PACKAGES = Join-Path $env:USERPROFILE '.nuget\\packages'; powershell -ExecutionPolicy Bypass -File dev.ps1 test long-term -Fresh`.
Observed output: bridge build succeeded with 0 warnings and 0 errors; RimWorld
PID 14952 was stopped; a new `-quicktest` launch began; the command remained at
`Waiting for bridge world and map readiness (timeout 180s)...` when the
orchestration attempt was interrupted. No suite response, assertion counts,
negative-control failure, restored re-verification, or clean-log result was
observed, so this evidence does not claim the Contract proven.
